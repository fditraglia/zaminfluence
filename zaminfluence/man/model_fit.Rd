% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_grads_lib.R
\name{model_fit}
\alias{model_fit}
\title{Construct a model_fit S3 object.}
\usage{
model_fit(
  fit_object,
  num_obs,
  param,
  se,
  parameter_names = NULL,
  weights = NULL,
  se_group = NULL
)
}
\arguments{
\item{fit_object}{The underlying fit object (e.g. the return value of
\code{\link[=lm]{lm()}}, \code{\link[ivreg:ivreg]{ivreg::ivreg()}}, or \code{\link[=glm]{glm()}}).}

\item{num_obs}{Integer. The number of observations used in the fit.}

\item{param}{Numeric vector of point estimates.}

\item{se}{Numeric vector of standard errors, one per entry of \code{param}.}

\item{parameter_names}{Character vector of parameter names. If \code{NULL},
defaults to \code{theta1}, \code{theta2}, ....}

\item{weights}{Numeric vector of length \code{num_obs}. If \code{NULL}, defaults
to all 1s.}

\item{se_group}{Optional grouping vector of length \code{num_obs} used for
grouped (clustered) standard errors.}
}
\value{
A validated \code{model_fit} object.
}
\description{
A \code{model_fit} bundles the outputs of a single model fit in a form that the
rest of \code{zaminfluence} can consume. Instances are normally produced by
\code{\link[=compute_model_influence]{compute_model_influence()}} and its dispatchers; call this constructor
directly only when wiring up a new model backend.
}
