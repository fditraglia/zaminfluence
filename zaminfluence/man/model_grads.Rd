% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_grads_lib.R
\name{model_grads}
\alias{model_grads}
\title{Construct a model_grads S3 object.}
\usage{
model_grads(model_fit, param_grad, se_grad, rerun_fun)
}
\arguments{
\item{model_fit}{A \code{model_fit} object at the original weights.}

\item{param_grad}{Numeric matrix of parameter gradients.
Dimensions are \verb{k x num_obs}, where \code{k} is the number of kept parameters
and \code{num_obs} is the number of observations. Rownames must be the kept
parameter names (a subset of \code{model_fit$parameter_names}).}

\item{se_grad}{Numeric matrix of standard-error gradients, same shape as
\code{param_grad}. Rownames must match \code{param_grad}.}

\item{rerun_fun}{Function taking a weight vector and returning a
\code{model_fit} at those weights.}
}
\value{
A validated \code{model_grads} object.
}
\description{
A \code{model_grads} packages a fitted model together with the gradients of its
parameters and standard errors with respect to the observation weights.
Instances are normally produced by \code{\link[=compute_model_influence]{compute_model_influence()}}; call this
constructor directly only when wiring up a new model backend.
}
